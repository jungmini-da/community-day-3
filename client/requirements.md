## Packages
html2canvas | Needed to capture the result card as a downloadable image
framer-motion | Needed for smooth quiz step transitions and premium animations

## Notes
The application requires `lucide-react` for the Flame icon (replaces static campfire.png).
No additional backend integration is required. The API endpoints for participants are assumed to be ready.
